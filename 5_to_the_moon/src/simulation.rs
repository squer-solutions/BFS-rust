mod physics;

fn run_simulation() {}

fn update_simulation() {
    // let force = velocity * -dragForce;

    // let acceleration = - estimate_gravitational_force();

    // let force += thrust;

    // acceleration += force / mass;

    // velocity += acceleration;

    // position += velocity;
}
